This Is a to generate a 2D Plane and generate 10 coordinates for ships to be placed into , after which distance between the all the ships are measured and the shortest distance 
between two ships is returned along with each of the ships x,y coordinates. It uses pytest to test for a predefined value.
